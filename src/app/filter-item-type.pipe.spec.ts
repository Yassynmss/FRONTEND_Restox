import { FilterItemTypePipe } from './filter-item-type.pipe';

describe('FilterItemTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemTypePipe();
    expect(pipe).toBeTruthy();
  });
});
